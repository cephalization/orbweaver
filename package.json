{
  "name": "orbweaver",
  "version": "1.0.0",
  "description": "Orbweaver is a library for rendering blob-like shapes.",
  "main": "index.js",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm --filter=orbweaver-core build && pnpm --filter=\"...^orbweaver-core\" build",
    "clean": "pnpm -r clean && rimraf node_modules",
    "ci:publish": "pnpm publish -r",
    "ci:check": "pnpm -r ci:check"
  },
  "keywords": [],
  "author": "Anthony Powell <powell.anthonyd@proton.me>",
  "license": "MIT",
  "packageManager": "pnpm@10.14.0",
  "devDependencies": {
    "@changesets/cli": "^2.29.5",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2"
  }
}
